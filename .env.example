# Neon Postgres connection string.
# In production Vercel injects DATABASE_URL automatically when you connect a Neon database.
# For local dev, paste the connection string from your Neon dashboard (free tier is fine).
DATABASE_URL=postgres://user:password@ep-xxxx-pooler.region.aws.neon.tech/neondb?sslmode=require
PORT=3000
